apiVersion: v1
kind: Service
metadata:
  name: theclaw
  labels:
    app: theclaw
spec:
  type: ClusterIP
  selector:
    app: theclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
