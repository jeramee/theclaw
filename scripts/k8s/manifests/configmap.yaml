apiVersion: v1
kind: ConfigMap
metadata:
  name: theclaw-config
  labels:
    app: theclaw
data:
  theclaw.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.theclaw/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "TheClaw Assistant",
            "workspace": "~/.theclaw/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # TheClaw Assistant

    You are a helpful AI assistant running in Kubernetes.
